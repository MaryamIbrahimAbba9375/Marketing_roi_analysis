# Simple Linear Regression - Marketing ROI Analysis

## Project Overview
This project constructs an Ordinary Least Squares (OLS) Simple Linear Regression model using Python and `statsmodels` to evaluate channel performance and drive optimal marketing budget allocation.

## Environment Setup
Ensure you have the required dependencies installed before executing the script:
```bash
pip install pandas numpy matplotlib seaborn statsmodels scipy
